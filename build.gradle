 plugins {
     id 'java'
 }

 subprojects {
     if (it.name != 'adrestore-be-spring') {
         group = 'hu.aestallon'
         version = '0.0.2'

         java {
             toolchain {
                 languageVersion.set(JavaLanguageVersion.of(17))
             }
         }
     }
 }